defaults: &defaults
  adapter: mysql
  username: root
  password: 
  host: localhost

development:
  database: mh_common_test
  <<: *defaults

test:
  database: mh_common_test
  <<: *defaults

production:
  database: mh_common_test
  <<: *defaults
